public with sharing class Testing {
    public void sampleTestMethod(Integer num) { 
        if (num > 5) {
            System.debug('Num is greater than 5');
        }
        System.debug('Testing new method');
    }
}